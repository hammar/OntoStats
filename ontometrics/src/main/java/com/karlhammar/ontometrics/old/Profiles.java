package com.karlhammar.ontometrics.old;

import java.io.File;
/*
import org.semanticweb.owlapi.apibinding.OWLManager;
import org.semanticweb.owlapi.model.OWLOntology;
import org.semanticweb.owlapi.model.OWLOntologyCreationException;
import org.semanticweb.owlapi.model.OWLOntologyManager;
import org.semanticweb.owlapi.profiles.OWL2ELProfile;
import org.semanticweb.owlapi.profiles.OWL2QLProfile;
import org.semanticweb.owlapi.profiles.OWL2RLProfile;
import org.semanticweb.owlapi.profiles.OWLProfileReport;
*/
public class Profiles {
	/*
	private File ontologyFile;
	OWLOntology ontology;
	OWLOntologyManager manager;
	
	public Profiles(File ontologyFile) throws OWLOntologyCreationException {
		super();
		this.ontologyFile = ontologyFile;
		this.manager = OWLManager.createOWLOntologyManager();
		this.ontology = manager.loadOntologyFromOntologyDocument(this.ontologyFile);
	}
	
	
	public Boolean isInElProfile() {
		OWL2ELProfile o2el = new OWL2ELProfile();
		OWLProfileReport report = o2el.checkOntology(this.ontology);
		return report.isInProfile();
	}
	
	
	public Boolean isInQlProfile() {
		OWL2QLProfile o2ql = new OWL2QLProfile();
		OWLProfileReport report = o2ql.checkOntology(this.ontology);
		return report.isInProfile();
	}
	
	
	public Boolean isInRlProfile() {
		OWL2RLProfile o2rl = new OWL2RLProfile();
		OWLProfileReport report = o2rl.checkOntology(this.ontology);
		return report.isInProfile();
	}*/
}
